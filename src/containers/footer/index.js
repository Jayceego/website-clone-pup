import PupLinks from "@/components/pup-links";

function FooterPup() {
  return (
    <div className="pt-[60px] container bg-primary-light">
      <PupLinks />
    </div>
  );
}

export default FooterPup;
